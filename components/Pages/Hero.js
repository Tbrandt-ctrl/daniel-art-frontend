import HeroStyle from "../../styles/Pages/Hero.module.scss";

const Hero = () => {
  return (
    <div className={`${HeroStyle.container}`}>
      <div>
        <h1>DANIEL BRANDT</h1>
      </div>
    </div>
  );
};

export default Hero;
